services:
  wsjtx-services:
    build: 
      context: .
      dockerfile: MessagePublisher/Dockerfile
      args:
        - GITHUB_USERNAME=${GITHUB_USERNAME}
        - GITHUB_TOKEN=${GITHUB_TOKEN} 
      

